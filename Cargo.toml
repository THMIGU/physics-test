[package]
name = "physics-test"
version = "0.1.0"
edition = "2024"

[dependencies]
sdl3 = {
    version = "0.18.3",
    features = ["build-from-source-static"]
}
rapier2d = {
	version = "0.32.0",
	features = [ "simd-stable" ]
}
